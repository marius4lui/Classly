# Docker Compose Deployment für Classly
# Nutzung:
# 1. Image bauen und starten: docker compose up -d --build
# 2. Logs anschauen: docker compose logs -f
# 3. Stoppen: docker compose down

services:
  classly:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./classly.db:/data/classly.db/classly.db
    environment:
      - PORT=8000
      - DATABASE_URL=sqlite:////data/classly.db
      # - SECRET_KEY=changeme
      # - TZ=Europe/Berlin
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  classly_data:
