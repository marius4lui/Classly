# Commit Message – v1.0.2

```
docs: add docker run installation guide & publish official image (v1.0.2)

- Publish marius4lui/classly:latest to Docker Hub
- Add new primary installation.md based on docker run (no git clone required)
- Rename old build-from-source guide to installation-legacy.md
- Add image: classly to docker-compose.yml to fix default image naming
- Add cp .env.example .env step to installation, configuration, contributing docs and README
- Add v1.0.2 entry to docs/versions and docs sidebar
```
